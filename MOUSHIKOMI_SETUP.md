# 入居申込フォーム セットアップ手順

入居申込を**ひとつの電子フォーム**で受け取り、**4つの保証会社**の審査にそのまま転記でき、
同時に**契約書類4点**まで自動作成する仕組みです。

## できること

| # | 内容 |
|---|------|
| 1 | 入居申込を1つのフォーマットで電子申込として受け取る（スマホ対応） |
| 2 | 本人確認書類の**表面・裏面**を写真でアップロード → 申込書と一緒にドライブへ保存 |
| 3 | 4社（日本セーフティー／全保連／ナップ／JID）の**転記シート**を自動作成 |
| 4 | **どの保証会社に流すかは担当者がボタンで選択**して、メールまたはFAXで審査依頼 |
| 5 | **見積書・請求書・賃貸借契約書・鍵受領書**を自動作成（すべてPDF） |
| 6 | 賃貸借契約書は**物件ごとの雛形**を登録して差し込み印刷（紙でのやりとり用にPDF出力） |

## ファイル構成

| ファイル | 役割 |
|---------|------|
| `moushikomi.html` | 入居申込フォーム（お客様が入力する画面） |
| `moushikomi-kanri.html` | 管理コンソール（保証会社への送信・書類作成・物件マスター） |
| `gas/moushikomi.gs` | バックエンド：申込受付・転記シート・保証会社送信 |
| `gas/moushikomi-shorui.gs` | バックエンド：物件マスター・契約書類4点の作成 |

---

## 1. Googleドライブにフォルダを作る

1. Googleドライブで新しいフォルダを作成します（例：`入居申込`）。
2. フォルダを開き、URLの `folders/` 以降の文字列（フォルダID）を控えます。

```
https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^ これがフォルダID
```

案件ごとに、このフォルダの中へ自動でサブフォルダが作られます。

```
入居申込/
├─ 入居申込受付一覧          （スプレッドシート・全項目を自動記録）
├─ 物件マスター              （スプレッドシート・物件ごとの条件と契約書雛形）
└─ 20260803_○×ハイツ_101_大森太郎/
   ├─ 入居申込書/            統一入居申込書PDF
   ├─ 保証会社転記シート/     4社分のPDF＋FAX送付状
   ├─ 本人確認書類/          表面・裏面の写真、その他の書類
   └─ 契約書類/              見積書・請求書・賃貸借契約書・鍵受領書
```

## 2. Google Apps Script を作る

1. [script.google.com](https://script.google.com/) を開き、**新しいプロジェクト**を作成します。
2. 既定の `コード.gs` の中身をすべて削除し、`gas/moushikomi.gs` の内容を貼り付けます。
3. 左の**ファイル ＋** から**スクリプト**を追加し、名前を `moushikomi-shorui` にして、
   `gas/moushikomi-shorui.gs` の内容を貼り付けます。
4. `moushikomi.gs` の先頭にある `M_CONFIG` を編集します。

```js
const M_CONFIG = {
  NOTIFY_EMAIL: 'marina.oomori1@gmail.com',  // 通知メールの宛先
  FOLDER_ID: 'ここに手順1のフォルダIDを貼り付け',
  ADMIN_PASSWORD: 'marina2026',              // 管理コンソールのパスワード（必ず変更してください）
  ...
};
```

5. 保存します（Ctrl+S／⌘+S）。

## 3. 保証会社の送信先を設定する

`moushikomi.gs` の `GUARANTORS` に、各社の審査申込の送信先を設定します。

```js
const GUARANTORS = [
  { key: 'ns',  name: '日本セーフティー株式会社', short: '日本セーフティー',
    email: '',                       // ← 審査申込のメール送信先
    faxEmail: '',                    // ← インターネットFAXの送信先
    fax: '' },                       // ← 審査専用FAX番号（送付状に印字）
  ...
];
```

### FAXについて（重要）

**Google Apps Script から電話回線のFAXを直接送ることはできません。**
FAXで送る場合は、次のどちらかになります。

| 方法 | 設定 | 動作 |
|------|------|------|
| インターネットFAX | `faxEmail` に送信先アドレスを設定<br>（例：`05030002321@fax.example.jp`） | 「📠 FAXで送信」ボタンで自動送信 |
| 手動でFAX | 設定不要 | 「📄 送付状PDFを作成」ボタンでFAX送付状を作成 → 印刷して送信 |

インターネットFAXサービス（eFax、MOVFAX、秒速FAX など）を契約すると、
指定のメールアドレス宛に送るだけでFAX送信できるようになります。
そのアドレスを `faxEmail` に設定してください。

未設定でも「送付状PDFを作成」は使えるので、まずはそちらで運用を始められます。

## 4. ウェブアプリとして公開する

1. 右上の**デプロイ** → **新しいデプロイ**。
2. 歯車アイコン → **ウェブアプリ** を選択。
3. 次のとおり設定します。

| 項目 | 設定値 |
|------|--------|
| 説明 | 入居申込フォーム |
| 次のユーザーとして実行 | **自分** |
| アクセスできるユーザー | **全員** |

4. **デプロイ**を押し、初回は**アクセスを承認**します。
   「このアプリは確認されていません」と出たら、**詳細** → **（プロジェクト名）に移動** → **許可**。
5. 表示された**ウェブアプリのURL**をコピーします。

```
https://script.google.com/macros/s/AKfycb.../exec
```

## 5. HTMLにURLを設定する

`moushikomi.html` と `moushikomi-kanri.html` の両方に、同じURLを貼り付けます。

```js
// moushikomi.html（350行目あたり）
const GAS_URL = 'https://script.google.com/macros/s/AKfycb.../exec';

// moushikomi-kanri.html（150行目あたり）
const GAS_URL = 'https://script.google.com/macros/s/AKfycb.../exec';
```

## 6. 動作確認

GASエディタで、関数を選んで**実行**します。

| 関数 | 確認できること |
|------|--------------|
| `testMoushikomi` | 統一申込書＋4社の転記シート＋契約書類が作られ、通知メールが届く |
| `testCreateMaster` | 物件マスターがサンプル1件つきで作られる |
| `testBuildDocuments` | 契約書類4点だけを作成する |

ドライブに `TEST_...` フォルダができていれば成功です（確認後は削除してください）。

---

## 使い方

### お客様への案内

`moushikomi.html` のURLをお伝えします。物件情報はURLで事前入力できます。

```
moushikomi.html?b=○×ハイツ&bk=マルバツハイツ&r=101&rent=50000&kanri=3000
               &zip=7370821&pref=広島県&addr=呉市三条4-7-20&use=住居用
```

| パラメータ | 内容 | | パラメータ | 内容 |
|---|---|---|---|---|
| `b` | 物件名 | | `rent` | 家賃 |
| `bk` | 物件名フリガナ | | `kanri` | 管理費・共益費 |
| `r` | 号室 | | `park` | 駐車場 |
| `zip` | 郵便番号 | | `shiki` | 敷金・保証金 |
| `pref` | 都道府県 | | `rei` | 礼金 |
| `addr` | 住所 | | `use` | 物件用途 |
| `agent` | 取扱店名 | | `staff` | 担当者 |

### 申込を受けたら

1. 通知メールが届きます（統一入居申込書PDFと本人確認書類が添付されます）。
2. `moushikomi-kanri.html` を開き、パスワードを入力します。
3. 一覧から申込を選びます。

#### ① 保証会社へ審査を依頼する

**どの保証会社に流すかは、担当者がボタンで選びます（自動送信はしません）。**

各社ごとに3つのボタンがあります。

| ボタン | 動作 |
|--------|------|
| ✉ メールで送信 | その社の転記シート＋本人確認書類を添付してメール送信 |
| 📠 FAXで送信 | 同じ内容をインターネットFAXで送信（`faxEmail` 設定時のみ） |
| 📄 送付状PDFを作成 | FAX送付状を作成。印刷して手動でFAX送信 |

送信した社は「✓ 送信済み」と表示され、ステータスが「審査依頼済」になります。
複数社に出すこともできます（1社ずつボタンを押します）。

#### ② 審査結果を登録する

「✅ 承認」「❌ 否認」「⏹ キャンセル」で結果を記録します。メモも残せます。

#### ③ 契約書類

申込を受け付けた時点で、次の4点が自動作成されています。

| 書類 | 内容 |
|------|------|
| 見積書 | 初期費用のお見積り（日割賃料・前家賃・敷金・礼金・仲介手数料など） |
| 請求書 | 同じ明細に振込先と支払期限を付けたもの |
| 賃貸借契約書 | 物件ごとの雛形から差し込み。**紙でのやりとり用にPDF出力** |
| 鍵受領書 | 鍵の種別・本数・受領日・押印欄 |

金額を直したいときは、管理コンソールで仲介手数料などを入力して
「📝 契約書類を作り直す」を押します。空欄の項目は物件マスターの値が使われます。

---

## 物件マスターと契約書の雛形

契約書は物件ごとに書式が違うため、**物件ごとに雛形を登録**します。

### 物件を登録する

管理コンソールの「物件マスター」タブ →「＋ 物件を登録する」。

主な項目：

- 物件名／号室／所在地／物件用途／構造／床面積
- 家賃・管理費共益費・駐車場・敷金・礼金・敷引
- 契約期間・更新料・賃料支払日・支払方法
- 貸主名・貸主住所・貸主TEL
- 仲介手数料・火災保険料・鍵交換費用・室内清掃費（見積書・請求書に反映）
- 初回保証料率（％）… 月額賃料合計から保証委託料を自動計算
- 鍵種別・鍵本数（鍵受領書に反映）
- 振込先（請求書に反映）
- **契約書雛形DocID**

> **号室を空欄**にすると、その物件（棟）全体の既定値として使われます。
> 号室ごとに条件が違う場合は、号室を入れて個別に登録してください。

### 契約書の雛形を作る

1. Googleドキュメントで、その物件の契約書式を作ります（既存の書式をコピーしてもOK）。
2. 差し替えたい箇所に**差込タグ**を書きます。

```
本契約は、賃貸人 {{貸主名}} と賃借人 {{契約者氏名}} との間で、
{{物件名}} {{号室}}（{{物件所在地}}）について締結する。

賃料　　月額 {{家賃}}
契約期間　{{契約始期}} から {{契約終期}} まで（{{契約期間}}）
```

3. ドキュメントのURLからIDを取り出し、物件マスターの「契約書雛形DocID」に貼り付けます。

```
https://docs.google.com/document/d/1AbCdEfGhIjKlMnOp/edit
                                   ^^^^^^^^^^^^^^^^^ これがDocID
```

これで、その物件の申込があると雛形が差し込まれてPDF化されます。
**雛形を登録していない物件は、内蔵の標準レイアウトで契約書が作られます。**

### 使える差込タグ

物件マスターの「雛形タグ一覧」シートにも同じものが載っています。

| 分類 | タグ |
|------|------|
| 契約者 | `{{契約者氏名}}` `{{契約者フリガナ}}` `{{契約者生年月日}}` `{{契約者住所}}` `{{契約者電話}}` `{{契約者携帯}}` `{{契約者勤務先}}` `{{契約者勤務先電話}}` |
| 物件 | `{{物件名}}` `{{号室}}` `{{物件所在地}}` `{{物件用途}}` `{{構造}}` `{{床面積}}` |
| 金額 | `{{家賃}}` `{{管理費共益費}}` `{{駐車場}}` `{{月額賃料合計}}` `{{敷金}}` `{{礼金}}` `{{敷引}}` `{{更新料}}` |
| 期間・支払 | `{{契約日}}` `{{契約始期}}` `{{契約終期}}` `{{契約期間}}` `{{賃料支払日}}` `{{支払方法}}` `{{振込先}}` |
| 貸主 | `{{貸主名}}` `{{貸主住所}}` `{{貸主電話}}` `{{管理会社}}` |
| 関係者 | `{{緊急連絡先氏名}}` `{{緊急連絡先続柄}}` `{{緊急連絡先電話}}` `{{緊急連絡先住所}}` `{{連帯保証人氏名}}` `{{連帯保証人住所}}` `{{連帯保証人電話}}` `{{同居人一覧}}` |
| その他 | `{{保証会社}}` `{{鍵種別}}` `{{鍵本数}}` `{{特約事項}}` |

---

## 4社の申込書と統一フォーマットの対応

統一フォームは4社の項目をすべて含んでいます。転記シートでは、**各社の項目名・項目順・選択肢のまま**出力されるので、そのまま転記できます。

| 統一フォームの項目 | 日本セーフティー | 全保連 | ナップ | JID |
|---|---|---|---|---|
| 雇用形態 | 職業（12択） | 雇用形態（番号1〜15） | 雇用形態（10択） | 職業（10択） |
| お住まい | 持家／賃貸／親族同居／他 | －（欄なし） | 自己所有／家族所有／賃貸／社宅 | 自己所有／社宅・寮／賃貸・その他 |
| 物件用途 | 住居／店舗／事務所／駐車場／その他 | 住居用／住居学生用ほか8択 | 居住用／居住用学生ほか9択 | 住居用／学生プラン／事業用／駐車場／その他 |
| 生年月日 | **和暦（T・S・H・R）へ自動変換** | 西暦 | 西暦 | 西暦 |
| 収入 | 月収（万） | 年収（万円） | 年収（万円） | 月収・手取（万円） |
| 同居人 | 3名まで（超過分は通信欄） | 2名まで（超過分は別紙） | 制限なし | 2名まで（超過分は連絡事項） |
| 緊急連絡先の続柄 | 自由記入 | **親子／兄弟・姉妹／親族／その他に自動変換** | 自由記入 | 自由記入 |

各社にしかない項目も統一フォームに含めています。

- **日本セーフティー**：健康保険の種類、保証プラン（プラス1／パートナー）、継続保証料の支払方法、賃料支払日・支払方法
- **全保連**：姓名の分割入力、性別の「無回答」、運転免許証番号、入居人数（成人／未成年）、水道料・町（区）費、介護施設区分、戸建区分
- **ナップ**：国籍、住居区分、収納代行の有無、ナップ付帯商品、保証プラン（居住用／事業用）
- **JID**：従業員数、居住年数、緊急連絡先の種別（緊急連絡先のみ／連帯保証人兼／親権者）、利用保証商品、保証料率

> 各社の様式・選択肢は改定されることがあります。転記シートの内容は、提出前に最新の申込書と照合してください。

---

## 運用上の注意

- 管理コンソールのパスワード（`ADMIN_PASSWORD`）は、必ず初期値から変更してください。
- 本人確認書類は個人情報です。ドライブのフォルダは社内の必要な方のみに共有してください。
- 写真は端末側で長辺1600pxに縮小・JPEG圧縮してから送信されます（1ファイル10MBまで）。
- Gmailの送信上限は1日あたり無料アカウントで約100通です。添付が多い日はご注意ください。
- 保証会社への審査依頼は**自動送信されません**。必ず担当者が管理コンソールから送信してください。

## 困ったときは

| 症状 | 対処 |
|------|------|
| 送信できない | `GAS_URL` が両方のHTMLに設定されているか確認 |
| 「パスワードが違います」 | `M_CONFIG.ADMIN_PASSWORD` と入力値が一致しているか確認 |
| 書類が作られない | GASエディタで `testBuildDocuments` を実行し、エラー内容を確認 |
| 契約書が標準レイアウトになる | 物件マスターの「契約書雛形DocID」が空、または物件名・号室が申込と一致していない |
| 「FAX送信先が未設定です」 | `GUARANTORS` の `faxEmail` を設定するか、「送付状PDFを作成」を使う |
| GASを修正しても反映されない | **デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン「新バージョン」→ デプロイ** |
