# 月極駐車場 利用申込フォーム セットアップ手順

エクセルの「月極駐車場利用申込書（個人／法人）」を電子フォーム化した仕組み一式です。

## 全体の流れ

```
① 管理者：moushikomilink.html で**駐車場を選んで**URLを作成
        ↓ コピーしてSMS・メールで申込者へ送信
② 申込者：moushikomi.html（フォーム）に入力して送信
   ・個人／法人を選択（管理者がURLで固定することも可能）
   ・運転免許証（契約者本人）の**表・裏**をその場で撮影 or アップロード
   ・「個人情報の第三者機関への提供」「記載事項に相違なく申込」への同意（いずれも必須）
        ↓ 送信をもって申込受付が完了（受付番号を表示）
③ 自動処理（Google Apps Script）：
   ・エクセル様式どおりの「月極駐車場利用申込書」PDFを作成
     → ドライブの「申込書」フォルダに保存
     ファイル名は「受付年月日_駐車場名_区画_申込者名.pdf」
     （例：20260901_大森第5ビル駐車場_A-1_大森太郎.pdf）
   ・運転免許証の画像を「本人確認書類」フォルダに保存（申込ごとにフォルダを作成）
     ＋ 申込書PDFの2ページ目にも添付
   ・marina.oomori1@gmail.com へ通知メール（申込書PDF添付）
   ・申込者がメールを入力していれば受付完了メールを自動返信
   ・スプレッドシート「駐車場申込受付一覧」に記録
```

保存先はこちらのドライブフォルダです：
<https://drive.google.com/drive/folders/1ABCiNBAH840DjxNnR3YXjQCkWAaz098->

```
（上記フォルダ）
├── 申込書/                         … 申込書PDF
├── 本人確認書類/
│   └── 20260901_大森第5ビル駐車場_A-1_大森太郎/
│       ├── …_運転免許証表.jpg
│       └── …_運転免許証裏.jpg
├── 駐車場申込受付一覧              … 受付の記録（スプレッドシート）
└── 月極駐車場マスター              … ★賃料・保証料の一覧（ここを編集して改定します）
```

## ファイル構成

| ファイル | 役割 |
|---|---|
| `moushikomi.html` | 申込者向けの利用申込フォーム（個人・法人の両対応・免許証アップロード付き） |
| `moushikomilink.html` | 管理者用。駐車場を選んで送信用URL・ご案内文を作成 |
| `gas/moushikomi.gs` | Google Apps Script（申込書PDF作成・ドライブ保存・メール通知・マスター配信） |

---

## セットアップ（初回のみ・約10分）

### 1. Google Apps Script を作成する

1. **marina.oomori1@gmail.com** のGoogleアカウントでログインした状態で
   <https://script.google.com> を開く
2. 「新しいプロジェクト」をクリック
3. プロジェクト名を「駐車場申込フォーム」などに変更
4. エディタに最初から入っているコードを全部消して、
   このリポジトリの **`gas/moushikomi.gs` の中身を全部コピーして貼り付け**、保存（💾）

> ※ 解約通知フォーム（`gas/kaiyaku.gs`）とは**別のプロジェクト**にしてください。

### 2. 動作テスト（権限の承認）

1. エディタ上部の関数選択で **`testSetup`** を選んで「実行」
2. 初回は「承認が必要です」と出るので、アカウントを選択して「許可」
   （「このアプリはGoogleで確認されていません」と出たら
   「詳細」→「駐車場申込フォーム（安全ではないページ）に移動」→「許可」）
3. 実行が終わると：
   - ドライブの指定フォルダに **「月極駐車場マスター」** スプレッドシートができる（初期の賃料入り）
   - 「申込書」フォルダにテストPDFができる
   - 「駐車場申込受付一覧」スプレッドシートができる
   - marina.oomori1@gmail.com にテスト通知メールが届く
   ことを確認してください（テストPDFと行は削除してOK）
4. 実行ログに出る「マスターのURL」を控えておくと便利です

### 3. ウェブアプリとしてデプロイする

1. 右上の「デプロイ」→「新しいデプロイ」
2. 歯車アイコン →「ウェブアプリ」を選択
3. 設定：
   - 説明：任意（例：駐車場申込フォーム）
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**（重要！これでないとフォームから送信できません）
4. 「デプロイ」→ 表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/～/exec`）をコピー

### 4. 2つのHTMLにURLを設定する

`moushikomi.html` と `moushikomilink.html` の**両方**の `<script>` 内にある

```js
const GAS_URL = '';
```

に、手順3でコピーした**同じURL**を貼り付けます：

```js
const GAS_URL = 'https://script.google.com/macros/s/～/exec';
```

その後、コミットしてGitHubにプッシュ（GitHub Pagesに反映されるまで数分待つ）。

### 5. 送信テスト

1. ブラウザで `https://（GitHub PagesのURL）/moushikomilink.html` を開く
2. 駐車場を選んでURLを作成 →「URLをコピー」
3. そのURLを開き、テスト入力＋免許証の画像を選んで送信
4. 受付完了画面・通知メール・ドライブへのPDF／画像保存を確認

---

## 日々の使い方

1. `moushikomilink.html` を開く
2. **駐車場を選択**（マスターにない場合は「＋ その他の駐車場」で名称・賃料をその場で入力）
3. 必要なら**区画番号**・**仲介手数料**を入力
4. 申込区分（申込者が選ぶ／個人で固定／法人で固定）を選ぶ
5. 「URLをコピー」または「ご案内文をコピー」→ SMS・メールで申込者へ送信

申込者が送信すると、通知メールが届き、申込書PDFと免許証画像がドライブに保存されます。

---

## ★賃料の変更（マスターの編集）

賃料・保証料の改定、駐車場の追加・削除は **コードを触らずに** できます。

1. ドライブの保存先フォルダにある **「月極駐車場マスター」** スプレッドシートを開く
   （`moushikomilink.html` の「📊 月極駐車場マスターを開く」ボタンからも開けます）
2. 該当する行の金額を書き換える／行を追加する
3. **保存するだけで即座にフォームへ反映されます**
   （すでに発行済みのURLも、開いたときに最新の賃料を読み込みます）

### マスターの列

| 列 | 意味 | 例 |
|---|---|---|
| `ID` | 内部の識別子。URLに入るので**変更しない**（新規行は英数字で任意に付ける） | `omori5` |
| `駐車場名` | フォーム・申込書に表示される名称 | `大森第5ビル駐車場` |
| `プラン名` | 同じ駐車場で条件が分かれる場合のみ（空欄可） | `125cc以下` |
| `月額賃料(税込)` | 1台あたりの月額賃料 | `16000` |
| `保証金区分` | `保証料` か `敷金`（そのままフォーム・申込書の項目名になります） | `保証料` |
| `保証金・敷金` | 上の区分の金額 | `16550` |
| `仲介手数料(税込)` | 既定の仲介手数料（0でOK。リンク作成時の入力が優先されます） | `0` |
| `表示順` | 小さいほど上に表示 | `10` |
| `有効` | `FALSE` にすると一覧から消えます（募集停止時に使用） | `TRUE` |

### 初期登録されている駐車場

| 駐車場名 | 月額賃料（税込） | 保証料／敷金 |
|---|---:|---:|
| 大森第5ビル駐車場 | 16,000円 | 保証料 16,550円 |
| 大森第5ビル駐車場2F | 8,800円 | 保証料 16,550円 |
| 呉駅前西中央駐車場 | 18,000円 | **敷金** 18,000円 |
| 三条4丁目大森駐車場 | 15,000円 | 保証料 15,550円 |
| 松ヶ丘中谷ガレージ | 5,000円 | 保証料 5,550円 |
| エイホームビル駐車場 | 18,000円 | 保証料 18,550円 |
| エイホームビルバイクガレージ（125cc以下） | 2,000円 | 保証料 2,550円 |
| エイホームビルバイクガレージ（125cc超え） | 2,500円 | 保証料 3,050円 |
| エイホームビルバイクガレージ（400cc超え） | 3,500円 | 保証料 4,050円 |

> ※ 上記以外は「その他の駐車場」としてリンク作成時に自由入力できます。
> よく使うようになったらマスターに追加してください。

---

## カスタマイズ

- **賃料・保証料・駐車場の追加**：上記「マスターの編集」（コード変更は不要）
- **通知先メール・保存先フォルダ・会社情報の変更**：`gas/moushikomi.gs` 冒頭の `M_CONFIG` を編集
  → 編集後は GASエディタにも貼り直し、「デプロイ」→「デプロイを管理」→ ✏️ →
  バージョン「新バージョン」で**再デプロイ**（URLは変わりません）
- **フォームの項目変更**：`moushikomi.html` を編集
  （申込書PDFの項目も変える場合は `gas/moushikomi.gs` の
  `m_kojinRows_` / `m_houjinRows_` / `m_emgRows_` / `m_carRows_` も合わせて編集）
- **予備の駐車場一覧**：`moushikomi.html` / `moushikomilink.html` 内の `FALLBACK_LOTS`
  （GASに接続できないときだけ使われる保険なので、通常は編集不要です）
- **アップロード画像のサイズ**：`moushikomi.html` の `MAX_SIDE`（既定：長辺1600px・JPEG）

## URLパラメータ一覧（moushikomi.html）

| パラメータ | 意味 | 例 |
|---|---|---|
| `p` | 駐車場のID（マスターの `ID` 列）。`other` でマスター外 | `p=omori5` |
| `s` | 区画番号の事前入力 | `s=A-1` |
| `f` | 仲介手数料（円・税込） | `f=16000` |
| `k` | 申込区分の固定（`k`=個人 / `h`=法人。省略時は申込者が選択） | `k=h` |
| `n` | 駐車場名（`p=other` のときのみ使用） | `n=○○町駐車場` |
| `r` | 月額賃料（`p=other` のときのみ使用） | `r=12000` |
| `d` | 保証料・敷金（`p=other` のときのみ使用） | `d=12550` |
| `dl` | 保証金の区分（`p=other` のときのみ使用） | `dl=敷金` |

例：`moushikomi.html?p=omori5&s=A-1&f=16000`

> ※ `p` がマスターに存在するIDの場合、賃料・保証料は**常にマスターの最新値**が使われます
>（URLに古い金額が残っていても反映されません）。

## 補足

- 申込書PDFはエクセル様式（個人／法人）どおりの項目・並びで作成され、
  末尾に「媒介業者名：(有)仁方大森マリーナー／免許番号：広島県知事(3)第10326号」が入ります。
- 法人様式には、エクセルにはない **「所在地」** の欄を追加しています（契約手続きに必要なため）。
- 押印欄の代わりに、フォーム送信時の同意記録（2項目）をPDFに明記しています。
- 運転免許証の画像は端末側で長辺1600pxのJPEGに縮小してから送信されます
  （通信量を抑えるためで、文字は問題なく読み取れます）。
