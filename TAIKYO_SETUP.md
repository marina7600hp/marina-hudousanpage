# 退去精算ワークフロー セットアップ手順

解約フォームの次の段階（退去立会い → 修繕見積書 → 修繕精算書）を、
1つの管理コンソール（案件一覧つき）で完結させる仕組みです。

## 全体の流れ

```
解約フォーム（kaiyaku.html／別GAS）
      ↓ 受付
【退去精算 管理コンソール（taikyo.html）】案件一覧から対象者を選ぶ
  ① 退去立会い … 原状回復箇所を写真撮影＋借主負担費用を電子署名で同意
        → 原状回復箇所報告書PDF（写真・根拠資料）
        → 退去立会い同意書PDF（費用・署名）
  ② 修繕見積書 … 品目×数量×単価×借主負担率で自動計算
        → 御見積書PDF
  ③ 修繕精算書 … 敷金から返金/不足を自動計算
        → 退去清算書PDF
```

- 氏名・物件・費用は前段階から**自動で引き継がれます**（二重入力なし）。
- 保存フォルダは親フォルダ配下に**段階別サブフォルダを自動作成して分離**します。

## ログインパスワード

管理コンソール（taikyo.html）はパスワードで保護されています。

- **パスワード：`MarinA7600`**（大文字・小文字は区別されます）
- 「この端末では次回から省略」にチェックを入れると、次回以降は入力不要です
- 変更したい場合は、ご希望のパスワードをお知らせください（`taikyo.html` の
  `GATE_SHA` / `GATE_DJB2` を差し替えます）

※このパスワードは画面を開くための簡易的な鍵です。書類の実体はGoogleドライブに
あり、そちらはGoogleアカウントで保護されています。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `taikyo.html` | 退去精算 管理コンソール（案件一覧＋立会い＋見積＋精算） |
| `gas/taikyo.gs` | バックエンド（PDF作成・ドライブ保存・進捗管理・通知） |

保存先の親フォルダ（ドライブ）:
`https://drive.google.com/drive/folders/1EYL6CpLSXe6dU2oKPXzPUgLqiDYdJfi1`
この中に「原状回復箇所報告書」「退去立会い同意書」「修繕見積書」「修繕精算書」
「御請求書」「送付状」の各フォルダと「退去精算_進捗管理」シートが自動作成されます。

御請求書の入金先口座（もみじ銀行 呉中央支店 普通 No.3113449 エイホームトラスト株式会社）や
送付状の差出人（住所・FAX・担当）を変更する場合は、taikyo.gs 冒頭 `T_CONFIG` の
`INVOICE_BANK` / `COMPANY_ADDR_FULL` / `COMPANY_FAX` / `COMPANY_CONTACT` を編集してください。

---

## セットアップ（初回のみ）

### 1. GASプロジェクトを作成

1. **marina.oomori1@gmail.com** でログインして <https://script.google.com> を開く
2. 「新しいプロジェクト」→ 名前を「退去精算」などに変更
3. 既定のコードを消し、`gas/taikyo.gs` の中身を全部貼り付けて保存

> 解約フォームのGAS（kaiyaku.gs）とは**別プロジェクト**にしてください。

### 2. （任意）解約フォームと連携させる

解約済みの入居者を管理コンソールに取り込めるようにする設定です。不要ならスキップ可。

1. 解約フォームの保存先フォルダにある「解約通知受付一覧」スプレッドシートを開く
2. URLの `.../d/【この部分】/edit` がスプレッドシートIDです。コピー
3. taikyo.gs 冒頭 `T_CONFIG` の `KAIYAKU_LOG_ID: ''` に貼り付け

設定すると、解約フォームの案件が管理コンソール一覧の先頭に「取り込み（未取込）」
として表示され、タップで取り込めます。このとき**氏名・物件に加え、解約フォームで
選ばれた金額（室内清掃代・エアコン洗浄代＝通常/お掃除機能付き）も引き継がれ、
①-B 退去立会い同意書の初期値に反映**されます（立会いで調整も可能）。
未設定でも、管理コンソールで手入力して案件を作成できます。

### 3. 動作テスト（権限承認）

1. 関数選択で **`testTaikyo`** を選んで「実行」
2. 初回は権限承認 →「許可」（確認されていない旨が出たら詳細→移動→許可）
3. 親フォルダに各サブフォルダ・進捗シート・テスト案件のPDF一式ができることを確認
   （テストデータは削除してOK）

### 4. ウェブアプリとしてデプロイ

1. 「デプロイ」→「新しいデプロイ」→ 種類「ウェブアプリ」
2. 次のユーザーとして実行：**自分** ／ アクセス：**全員**
3. デプロイ → 表示された**ウェブアプリURL**をコピー

### 5. コンソールにURLを設定

1. `taikyo.html` を開き、`const GAS_URL = '';` にコピーしたURLを貼り付け
2. GitHubにアップロード（GitHub Pages反映まで数分）

### 6. 使ってみる

1. `https://（GitHub PagesのURL）/taikyo.html` を開く
2. 「＋ 新規案件」で案件を作成（または解約済みから取り込み）
3. 案件を開いて ①立会い →②見積 →③精算 の順に進める

---

## 日々の使い方（退去立会いの当日）

タブレットやスマホで `taikyo.html` を開き、対象の案件をタップします。

立会いは **報告書** と **同意書** を別ボタンで作成します（写真が後から増えても、
報告書だけ作り直せばよく、署名のやり直しは不要です）。

1. **①-A 原状回復箇所報告書（写真）**
   - 退去日・立会日・立会担当者を入力
   - 「📷 撮影して追加」または「🖼 写真から選択」で原状回復箇所を登録
     （**スマホの写真フォルダからも選べます**・複数可）。写真ごとに箇所名・損傷種別・状況メモを入力
   - 「原状回復箇所報告書を作成」→ 写真つきの報告書PDFと元写真がフォルダに保存されます
     （追加写真が出たら、写真を足して再度ボタンを押せば作り直せます）
2. **①-B 退去立会い同意書（費用・署名）**
   - 室内清掃費・エアコン洗浄費（台数）・畳表替え費用（枚数）を確認・入力（**税込**）
   - クロスは「㎡単価○○円で実測後に精算」に同意（その場で金額を出さなくてOK）
   - **「後日見積となる原状回復箇所」**を選択（鍵紛失のため交換費用／柱損傷／
     CFシート傷・汚損／剥がれ／フローリング破損・汚損／網戸破け／ヤニ汚れ、その他も追加可）。
     選択した箇所は「費用は後日見積のうえ支払うことを約束する」旨が同意書に記載され、署名で同意いただきます
   - 借主さまに署名欄へ**指で署名**していただき、「立会い同意書を作成」
3. **② 修繕見積書**：立会いの費用＋後日見積で選んだ箇所が初期表示。
   実測㎡数や単価・負担率を入れ、見積作成者を入力して作成
4. **③ 修繕精算書**：敷金・原状回復費（見積の借主負担額が初期表示）から返金/不足を自動計算。精算書作成者を入力。
   「精算書を作成」を押すと次の書類も**自動作成**されます。
   - **御請求書**（精算がマイナス＝**不足（請求額）が発生した場合のみ**）：借主へ原状回復費用の不足額を請求。
     入金先は もみじ銀行 呉中央支店 普通 No.3113449 エイホームトラスト株式会社。お支払期限を入れると請求書に記載されます
   - **送付状（書類送付御案内）**：見積書・清算書・（請求書）の同封案内。宛名は退去者、送付先は「書類送付先（転居先）」を使用。
     A4を上下half分に切り、下半分が控え。控え左下の枠（**70×40mm**）を切り取ると封筒の宛名ラベルになります
5. **④ 入金・完了**：返金振込（または請求分の着金）が済んだら**完了日を入力**して記録。
   請求（不足）の場合は**請求書郵送日**、**確認者**（自由記述）も入力できます。これで案件が「完了」になります

一覧では各案件の**進捗バー（％）**・状態・「次にやること」・立会日/退去日・担当者・完了日が
一目で分かります。案件詳細の上部には作成済みの書類（原状回復報告書／立会い同意書／
見積書／精算書）がタブで並び、タップで開けます。PC・スマホどちらでも見やすく表示されます。

## LINE通知の設定（任意）

各段階の完了をLINEに通知できます。**社内グループ**に加えて、**貸主ごとのグループLINE**にも
送れます（貸主を登録したものだけ）。設定しなければ何も起きません（メール通知のみ）。

> ※かつての「LINE Notify」は2025年3月末で終了したため、**LINE公式アカウント
> （Messaging API）**を使います。無料プランで月200通まで送信できます。

### 1. LINE公式アカウントとチャネルを作る

1. <https://developers.line.biz/console/> にLINEアカウントでログイン
2. 「新規プロバイダー作成」→ 会社名などを入力
3. 「新規チャネル作成」→ **Messaging API** を選択し、必要事項を入力
4. 作成後、「Messaging API設定」タブ →「チャネルアクセストークン（長期）」を**発行してコピー**
5. **トークンはコードに書かず、GAS内の安全な保管場所に登録します**
   （このリポジトリは公開のため、コードに書くと誰でも見られてしまいます）

   1. Apps Script の退去精算プロジェクトを開く
   2. 左メニューの **⚙️「プロジェクトの設定」**
   3. 下のほうの **「スクリプト プロパティ」→「スクリプト プロパティを追加」**
   4. プロパティ名：`LINE_CHANNEL_ACCESS_TOKEN` ／ 値：コピーしたトークン
   5. **「スクリプト プロパティを保存」**をクリック

   > この方法なら、あとでコードを最新版に貼り替えてもトークンは消えません。

### 2. Webhookを設定して、グループIDを調べる

グループへ送るには「グループID」が必要です。次の手順で自動的に調べられます。

1. 「Messaging API設定」の **Webhook URL** に、この退去精算GASの
   **ウェブアプリURL**（`https://script.google.com/macros/s/～/exec`）を設定し、
   「**Webhookの利用**」をオンにする
2. 同じ画面の「応答メッセージ」は**オフ**にしておくと、余計な自動返信が出ません
3. 通知したいグループLINEに、作成した**公式アカウントを招待**する
4. そのグループで何かメッセージを送る
5. 親フォルダに **「LINEグループID一覧」** シートが自動作成され、
   グループIDが記録されます（`C` で始まる長い文字列）

### 3. 送信先を設定する

社内グループのIDは、トークンと同じ「スクリプト プロパティ」に
`LINE_STAFF_TO` という名前で登録するのが簡単です（コード編集が不要になります）。

貸主ごとのグループは、taikyo.gs 冒頭の `T_CONFIG.LINE` に記入します。

```js
LINE: {
  CHANNEL_ACCESS_TOKEN: '',              // ← 空のままでOK（スクリプトプロパティを使用）
  STAFF_TO: '',                          // ← スクリプトプロパティ LINE_STAFF_TO を使う場合は空でOK
  OWNER_GROUPS: {                        // 貸主ごとのグループ（任意）
    'エイホームトラスト株式会社': 'Cxxxxxxxx',
    '大森栄作': 'Cyyyyyyyy',
  },
  OWNER_STAGES: { tachiai: true, estimate: true, settlement: true, complete: true },
  OWNER_INCLUDE_AMOUNT: true,            // false にすると貸主へは金額を送りません
},
```

- **貸主グループに送りたくない段階**は `OWNER_STAGES` を `false` に
- **貸主には金額を知らせたくない**場合は `OWNER_INCLUDE_AMOUNT: false` に
- `OWNER_GROUPS` に登録していない貸主の案件は、**社内グループにのみ**通知されます

### 4. テスト

関数 **`testLine`** を実行すると、社内グループにテストメッセージが届きます。
うまくいかない場合は、実行ログに原因（トークン未設定・宛先未設定など）が表示されます。

### トークンを変更・再発行したとき

「プロジェクトの設定 → スクリプト プロパティ」の `LINE_CHANNEL_ACCESS_TOKEN` の値を
書き換えるだけです。コードの修正・再デプロイは不要です。

### ご注意（貸主グループへ送る場合）

貸主とのグループLINEには、**入居者名・物件名・金額**が流れます。
入居者の個人情報にあたるため、送る範囲は貸主との取り決めのうえでご判断ください。
氏名を伏せたい等のご要望があれば、通知文面を調整できます。

## カスタマイズ（taikyo.gs 冒頭 `T_CONFIG`）

- `DEFAULT_CLEANING` / `DEFAULT_AC` / `DEFAULT_TATAMI` … 費用の初期値
- `DEFAULT_CROSS_UNIT` … クロス㎡単価（税別）
- `TAX_RATE` … 消費税率（既定 0.10）
- `NOTIFY_EMAIL` … 立会い・精算完了の通知先
- `PARENT_FOLDER_ID` … 保存先の親フォルダ
- 変更後は GASに貼り直し →「デプロイを管理」→ ✏️ →「新バージョン」で再デプロイ

## 補足

- 写真は端末内で縮小してから送信します（通信量を抑えるため）。原本は
  「原状回復箇所報告書」内の案件フォルダに画像として保存され、報告書PDFにも掲載されます。
- 署名は電子的な同意の記録です。契約・立会いの実務における同意の証跡として、
  同意書PDFに署名画像と日付が残ります。
