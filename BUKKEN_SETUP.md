# 物件掲載 入稿コンソール セットアップ手順

物件情報を**1回だけ入力**すれば、スーモ・スマイミー・きまるーむへの登録を
上から順に貼り付けていくだけで済むようにする仕組みです。
同じ内容を3回入力し直す手間をなくすことが目的です。

## 前提（各社に確認済み・2026年9月）

**3社ともCSVでの一括入稿には対応していない**との回答でした。
したがって各サイトへの登録は、管理画面への手入力になります。

そこでこのツールは「入力を1回にまとめ、転記を速くする」ことに絞っています。
CSV出力は**自社のバックアップ用**として残しています。

## 全体の流れ

```
① bukken.html を開いてパスワードを入力（他の管理ページと同じパスワードです）
        ↓
② 「＋ 新規」から物件を1回だけ入力
   ・敷金/礼金は「ヶ月」を入れると円が自動計算されます
   ・設備はチェックを入れるだけ
        ↓
③ 「書き出す」→「サイトへ転記する」
   ・転記先のサイトを選ぶ
   ・項目ごとの［コピー］を押して、管理画面に貼り付け
   ・貼り終えた項目はチェックを入れる（どこまで進んだか分かります）
        ↓
④ 次のサイトを選んで、同じように貼り付け
   （入力はもう不要。チェックはサイトごとに別々に記録されます）
```

## ファイル構成

| ファイル | 役割 |
|---|---|
| `bukken.html` | 画面本体（一覧・入力・転記） |
| `bukken-schema.js` | 物件の項目定義。**項目を増やしたいときだけ**触ります |
| `bukken-portals.js` | サイトごとの転記手順。**転記を速くしたいとき**に触ります |

データはお使いのパソコンのブラウザ内に保存されます。サーバーには送信されません。

> **バックアップのお願い**
> パソコンを買い替えるとき、ブラウザのデータを消したときに失われます。
> ときどき「自社用に保存する」タブからCSVに書き出して保管してください。
> 書き出したCSVは一覧画面の「マスターCSVを取り込む」で元に戻せます。

---

## 転記画面の使い方

### 転記済みチェック

貼り付けた項目にチェックを入れると、その行が薄くなり、上の進捗バーが進みます。
**チェックは物件ごと・サイトごとに分けて記録**されるので、
スーモの途中で中断してスマイミーを進めても混ざりません。
次に開いたときもチェックは残っています。

### 全部まとめてコピー

「項目名：値」の形で全項目をまとめてコピーします。
メモ帳に貼っておいて見ながら入力したいとき、
社内の別の方に内容を渡したいときにお使いください。

### 社内用の項目は出ません

管理番号・取引態様・元付会社・社内メモは、サイトに載せるものではないので
転記画面には表示されません（CSVバックアップには含まれます）。

### 未入力の項目は出ません

入力していない項目は転記画面に出てこないので、
上から順に貼っていけば終わります。

---

## 転記をもっと速くする（任意）

初期状態では、項目が `bukken-schema.js` の並び順で表示されます。
これを**各サイトの登録画面と同じ順番**に並べ替えると、
画面を行ったり来たりせず上から順に貼れるようになります。

実際にサイトの登録画面を開いて、出てくる順に `bukken-portals.js` の
`steps` を書いてください。

```js
suumo: {
  label: 'スーモ',
  csv: false,
  steps: [
    { section: '基本情報' },                        // 見出し（任意）
    { label: '建物名', from: 'buildingName' },      // サイト側の名称 : こちらの項目
    { label: '住所',   from: function (v) {         // 複数項目をつなげる場合
        return v.pref + v.city + v.town + v.banchi;
      } },
    { label: '最寄駅', from: 'sta1', note: '「駅」は付けない' },  // 注意書き（任意）
  ]
}
```

| 書き方 | 意味 |
|---|---|
| `section` | 見出しを入れたいとき |
| `label` | **そのサイトの画面に出ている項目名**を書きます |
| `from` | こちらの項目キー（`bukken-schema.js` に日本語ラベル付きの一覧があります） |
| `from: function(v){...}` | 複数の項目を組み合わせたいとき |
| `note` | 「全角で入れる」「単位は不要」など、間違えやすい点のメモ |

`steps` を空 `[]` のままでも問題なく使えます。急ぎでなければ、
実際に何度か転記してみて、面倒な並びだと感じたときに整えるのがおすすめです。

## 項目を増やしたいとき

`bukken-schema.js` の該当グループに1行足すだけです。
入力フォーム・転記画面・バックアップCSVすべてに自動で反映されます。

```js
{ key: 'newField', label: '新しい項目', type: 'text', placeholder: '例）...' },
```

`type` は `text` / `textarea` / `number` / `select` / `checkgroup` / `month` / `date` が使えます。

---

## まだ確認する価値があること

### きまるーむの「キマルーム Convert」

CSV入稿とは**別の仕組み**です。株式会社キマルームは「キマルーム Convert」という
物件コンバートを提供しており、登録した物件を**ワンクリックで SUUMO・HOME'S・
アットホーム・マイナビ賃貸へ転載**できると案内されています。

「CSVでの一括入稿は不可」という回答は、この Convert の可否とは別の話です。
これが使えるなら、スーモへの転記そのものが不要になります。

> 確認する内容：
> 「キマルーム Convert を利用したいのですが、料金と申込方法を教えてください。
> 　現在の契約から追加できますか。転載できるポータルサイトはどこですか。」

- サービス紹介：https://service.kimaroom.co.jp/service/convert/

### スマイミーと宅建協会システムの連携

スマイミー（`fudohsan.jp`）は**広島宅建株式会社**が運営し、広島県宅地建物取引業協会と
連携しており、掲載情報はハトマークサイト・不動産ジャパン・西日本レインズなどに
反映される仕組みと案内されています。

協会システムへの登録がスマイミーに反映されるなら、こちらも転記が不要になります。

> 確認する内容：
> 「協会のシステム（ハトマークサイト／レインズ）に登録した物件は、
> 　スマイミーにも自動で反映されますか。別途スマイミー専用の登録が必要ですか。」

※ 上記2件は検索で得た情報をもとにしています。必ず各社にご確認ください。
